$(document).ready(function () {
    console.log("ready!");
    $('#fromDatePicker').datepicker({
        format: 'mm/dd/yyyy'
    });

    $('#toDatePicker').datepicker({
        format: 'mm/dd/yyyy'
    });


});